interface:
  display_name: "Telegram Rich Content"
  short_description: "Telegram-копирайтер: мысль, голос, статья или видео"
  default_prompt: "Use $telegram-rich-content to turn this material into a Telegram post in my approved voice. Draft and validate it, but do not publish."
policy:
  allow_implicit_invocation: true
